## 1.1.0

- Add `--output="path"` parameter to all commands (`/generate`, `/edit`, `/restore`, `/icon`, `/pattern`, `/story`, `/diagram`) for specifying a custom output directory.
- Images are now saved to the user-specified folder instead of always defaulting to `./nanobanana-output/`.

## 1.0.1

- Fix `edit_file` tool to handle absolute file paths.

## 1.0.0

- Initial release.
